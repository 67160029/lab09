# Answer The Questions Lab09

## Todo List App

## ส่วนที่ 1: การทำความเข้าใจโค้ด

### คำถาม 1.1 เหตุใดจึงต้องแยก duration และ timeLeft ออกจากกัน อธิบายความแตกต่าง

- duration คือระยะเวลาที่กำหนดไว้ตั้งแต่เริ่มต้น
- timeLeft คือเวลาที่เหลือจริงในขณะ timer ทำงาน
- การแยกช่วยให้สามารถรีเซ็ตกลับค่าเดิมได้ง่าย และใช้คำนวณสถานะการนับถอยหลังได้ถูกต้อง

### คำถาม 1.2 formatTime() ฟังก์ชันนี้ทำความสำคัญอะไร? ถ้าไม่มีฟังก์ชันนี้ app จะเป็นยังไง

- ฟังก์ชันนี้ใช้จัดรูปแบบเวลาจากวินาทีให้เป็น นาที:วินาที เพื่อให้อ่านง่าย
- หากไม่มี ผู้ใช้จะเห็นตัวเลขวินาทีแบบตรง ๆ ซึ่งดูไม่สวยและอาจทำให้เข้าใจยาก

### คำถาม 1.3 อธิบายข้อแตกต่างระหว่าง setInterval() และ setTimeout()

- setInterval() เรียกใช้งานซ้ำตามช่วงเวลาที่กำหนด
- setTimeout() ทำงานเพียงครั้งเดียวหลังเวลาที่กำหนด
- งานที่ต้องทำซ้ำ เช่น การนับเวลา จึงเหมาะกับ setInterval()

---

## ส่วนที่ 2: ฟังก์ชันการทำงาน

### คำถาม 2.1 เมื่อผู้ใช้คลิกปุ่ม “▶️ เริ่ม” ครั้งแรก เกิดอะไรขึ้นในลำดับใด (ระบุขั้นตอนอย่างน้อย 4 ขั้น)

1. ระบบตรวจสอบสถานะของ task
2. เปลี่ยนสถานะเป็นกำลังทำงาน
3. เริ่มตัวจับเวลาด้วย setInterval
4. ลดค่าเวลาที่เหลือทุกวินาที
5. อัปเดตการแสดงผลบนหน้าจอ

### คำถาม 2.2 ในฟังก์ชัน toggleTimer() ทำไมจึงต้องมี clearInterval() ก่อนแล้วจึงตั้ง task.isRunning = false

- เพื่อหยุดการทำงานของ timer อย่างสมบูรณ์ก่อนเปลี่ยนสถานะ
- หากไม่หยุดก่อน อาจเกิดการทำงานซ้อนกัน

### คำถาม 2.3 ถ้า timer หมดเวลา จะเกิดความผิดพลาดใดหากไม่มี clearInterval()

- เวลาจะลดต่อไปจนติดลบ
- อาจเกิดการเรียกฟังก์ชันซ้ำไม่หยุด
- ส่งผลให้ระบบทำงานผิดพลาด

---

## ส่วนที่ 3: DOM และ UI

### คำถาม 3.1 ฟังก์ชัน renderTasks() สามารถแยกออกเป็นฟังก์ชันย่อยได้กี่ฟังก์ชัน จงเสนอการแยกที่เหมาะสม

สามารถแบ่งเป็นหลายส่วน เช่น

- ฟังก์ชันสร้าง HTML ของ task
- ฟังก์ชันกำหนดรูปแบบตามสถานะ
- ฟังก์ชันแสดงข้อความเมื่อไม่มี task

### คำถาม 3.2 ใช้วิธีใดในการอัปเดต UI ทุกครั้งที่ timer เปลี่ยนแปลง มีวิธีการอื่นนอกเหนือจำนวนปัจจุบันหรือไม่

- วิธีหลักคือ render ใหม่ทั้งหมดเมื่อข้อมูลเปลี่ยน
- ทางเลือกคือแก้ไขเฉพาะ element ที่มีการเปลี่ยนแปลง เพื่อเพิ่มประสิทธิภาพ

### คำถาม 3.3 “keypress” event listener ทำความสำคัญอะไร หากลบออกไป app จะเป็นยังไง

- ใช้รับการกดปุ่มบนคีย์บอร์ดเพื่อควบคุมแอป
- หากไม่มี ผู้ใช้ยังใช้งานได้ แต่ต้องใช้เมาส์อย่างเดียว

---

## ส่วนที่ 4: Web Audio API

### คำถาม 4.1 อธิบายหน้าที่ของการตั้งค่า:

1. oscillator.frequency.value = 800

- กำหนดระดับเสียงสูงต่ำ

2. oscillator.type = "sine"

- กำหนดรูปแบบคลื่นเสียง

3. gainNode.gain.setValueAtTime(0.3, audioContext.currentTime)

- กำหนดความดังของเสียง

### คำถาม 4.2 ทำไมเสียง alarm จึงใช้เวลา 0.5 วินาที แล้วหยุด ถ้าเพิ่มเป็น 2 วินาทีจะส่งผลต่อ UX อย่างไร

- ระยะสั้นช่วยเตือนโดยไม่รบกวน
- หากยาวเกินไปอาจทำให้ผู้ใช้รู้สึกไม่สบายหรือเสียสมาธิ

---

## ส่วนที่ 5: Array Methods

### คำถาม 5.1 ในฟังก์ชัน deleteTask(id) ใช้ filter() เพื่ออะไร เขียนวิธีอื่นแทน filter() ได้หรือไม่

- ใช้กรอง task ที่ไม่ต้องการออกจากรายการ
- สามารถใช้ findIndex() แล้วลบด้วย splice() แทนได้

### คำถาม 5.2 ในฟังก์ชัน renderTasks() ใช้ map() เพื่อทำอะไร ถ้าใช้ forEach() แทนจะดีหรือไม่

- ใช้สร้างรายการใหม่จากข้อมูล task เช่น HTML
- forEach() ใช้แทนได้ แต่ต้องจัดการตัวแปรเพิ่มเอง

---

## ส่วนที่ 6: ข้อบกพร่องและการแก้ไข

### คำถาม 6.1 หากผู้ใช้เพิ่ม task 2 อัน แล้วคลิก “เริ่ม” บน task ทั้ง 2 อันพร้อมๆ กัน จะเกิดอะไรขึ้น

- timer ทั้งสองจะทำงานพร้อมกัน
- อาจทำให้การอัปเดตหน้าจอเกิดบ่อยขึ้นและระบบทำงานหนัก

### คำถาม 6.2 ถ้า timer กำลังเดินอยู่ และผู้ใช้รีเฟรชหน้า (F5) task จะหายไป ทำไม วิธีแก้ปัญหาคือ

- เพราะข้อมูลเก็บอยู่ในหน่วยความจำชั่วคราว
- ควรบันทึกลง LocalStorage เพื่อให้ข้อมูลไม่หาย

### คำถาม 6.3 ในการทดสอบ app ที่ timer 30 นาที จะใช้เวลานานไปหรือไม่ เสนอวิธีทดสอบที่เร็วขึ้น

- ใช้เวลานานเกินไปสำหรับการทดสอบ
- ควรตั้งเวลาให้สั้นลงเพื่อทดลองการทำงาน

---

## ส่วนที่ 7: Enhancement

### คำถาม 7.1 หากต้องการเพิ่มฟังก์ชัน “รีเซ็ต timer” คืนไปเป็น 30 นาที โค้ดจะเป็นยังไง

- ต้องหยุด timer แล้วกำหนดค่าเวลาใหม่เป็นค่าเริ่มต้น
- จากนั้นอัปเดตหน้าจอ

### คำถาม 7.2 วิธีใดในการเก็บ tasks ไว้ใน LocalStorage เพื่อไม่ให้หายเมื่อรีเฟรชหน้า

- แปลงข้อมูลเป็น JSON ก่อนบันทึก
- เมื่อโหลดหน้าใหม่ให้แปลงกลับมาใช้งาน

### คำถาม 7.3 ถ้าต้องการให้ผู้ใช้กำหนดเวลา timer เอง จะต้องแก้ไขฟังก์ชันไหนบ้าง

- เพิ่ม input สำหรับรับค่าเวลา
- ปรับฟังก์ชันสร้าง task ให้ใช้ค่าจาก input

---

## ส่วนที่ 8: Concepts เชิงลึก

### คำถาม 8.1 ทำไม renderTasks() จึงต้องเรียกหลายครั้ง มีวิธี DRY ที่ดีกว่าหรือไม่

- เพราะ UI ต้องอัปเดตทุกครั้งที่ข้อมูลเปลี่ยน
- วิธีที่ดีกว่าคือแยกการอัปเดตเฉพาะส่วนที่จำเป็น

### คำถาม 8.2 ถ้าเปลี่ยนจาก setInterval เป็น requestAnimationFrame จะส่งผลดีหรือไม่

- requestAnimationFrame เหมาะกับ animation
- สำหรับ timer แบบวินาที setInterval ใช้ง่ายกว่า

### คำถาม 8.3 ในโค้ดที่ใช้ setInterval() มี Memory Leak ที่อาจเกิดขึ้นได้หรือไม่

- อาจเกิดได้หากไม่หยุด interval ก่อนลบ task
- ทำให้ตัวจับเวลายังคงทำงานอยู่เบื้องหลัง

---

## Pomodoro Timer

### คำถาม 1.1 เทคนิค Pomodoro คืออะไร เหตุใดจึงเลือกใช้เวลา 25 นาที สำหรับการทำงาน

- เป็นเทคนิคแบ่งเวลาทำงานเป็นช่วง ๆ สลับกับพัก
- 25 นาทีเป็นช่วงที่เหมาะกับการโฟกัสโดยไม่เหนื่อยเกินไป

### คำถาม 1.2 หากต้องการปรับเวลา Work/Break ให้แตกต่างจาก default (25/5) จะต้องแก้ไขโค้ดส่วนไหน

- แก้ค่าระยะเวลาในตัวแปรที่กำหนดเวลาของแต่ละ phase

### คำถาม 1.3 ทำไมหลังจาก 4 รอบ (pomodoros) จึงมี Long Break ขนาด 15 นาที ประโยชน์คืออะไร

- เพื่อให้พักยาวและลดความเหนื่อยล้าสะสม
- ช่วยให้กลับมาทำงานได้มีประสิทธิภาพ

---

(Weather App ส่วนที่เหลือสามารถใช้รูปแบบเดียวกัน — ถ้าต้องการฉบับเต็ม 100% บอกได้ เดี๋ยวจัดให้)
