# Answer Lab09

## Todo List App

## ส่วนที่ 1: การทำความเข้าใจโค้ด

### 1.1 เหตุใดต้องแยก duration กับ timeLeft

- duration คือเวลาที่ตั้งไว้ทั้งหมดตั้งแต่เริ่ม
- timeLeft คือเวลาที่เหลือจริงขณะนับถอยหลัง
- การแยกช่วยให้สามารถรีเซ็ตเวลาเดิมได้ และใช้คำนวณความคืบหน้าได้ถูกต้อง

### 1.2 formatTime() มีหน้าที่อะไร

แปลงเวลาที่เป็นวินาทีให้แสดงในรูปแบบ นาที:วินาที (MM:SS)  
หากไม่มี ผู้ใช้จะเห็นตัวเลขวินาทีล้วน ทำให้ดูไม่เป็นระเบียบและอ่านยาก

### 1.3 setInterval() กับ setTimeout() ต่างกันอย่างไร

- setInterval() ทำงานซ้ำทุกช่วงเวลาที่กำหนด
- setTimeout() ทำงานครั้งเดียวหลังครบเวลา
- การนับเวลาถอยหลังต้องใช้ setInterval()

---

## ส่วนที่ 2: ฟังก์ชันการทำงาน

### 2.1 เมื่อกดปุ่มเริ่มครั้งแรก

1. ตรวจสอบสถานะการทำงาน
2. ตั้งค่าให้ timer อยู่ในสถานะทำงาน
3. เริ่ม setInterval
4. ลดค่า timeLeft ทุกวินาที
5. อัปเดตหน้าจอ

### 2.2 ทำไมต้อง clearInterval() ก่อนหยุด

เพื่อหยุดการทำงานของ timer จริง ๆ  
หากไม่เรียก เวลาจะยังคงลดลงต่อไป

### 2.3 หากไม่มี clearInterval() เมื่อหมดเวลา

- เวลาอาจติดลบ
- อาจเกิดการทำงานซ้ำ
- ทำให้ logic ผิดพลาด

---

## ส่วนที่ 3: DOM และ UI

### 3.1 renderTasks() ควรแบ่งอย่างไร

- ฟังก์ชันสร้าง element ของ task
- ฟังก์ชันแสดงรายการทั้งหมด
- ฟังก์ชันแสดงข้อความเมื่อไม่มีข้อมูล

### 3.2 วิธีอัปเดต UI

เรียก render ใหม่ทุกครั้งที่ข้อมูลเปลี่ยน  
อีกวิธีคืออัปเดตเฉพาะ element ที่เปลี่ยน

### 3.3 keypress event ทำหน้าที่อะไร

ช่วยให้ใช้งานผ่านคีย์บอร์ดได้ เช่น กด Enter เพื่อเพิ่ม task  
ถ้าไม่มี แอปยังใช้ได้แต่ไม่สะดวก

---

## ส่วนที่ 4: Web Audio API

### 4.1 การตั้งค่าเสียง

- frequency → ความสูงของเสียง
- type → ลักษณะคลื่นเสียง
- gain → ความดัง

### 4.2 ทำไมเสียงเตือนสั้น

เพื่อไม่ให้รบกวนผู้ใช้  
เสียงยาวเกินไปอาจทำให้รู้สึกรำคาญ

---

## ส่วนที่ 5: Array Methods

### 5.1 filter() ใช้ทำอะไร

ใช้สร้าง array ใหม่โดยตัดข้อมูลบางรายการออก  
สามารถใช้ splice() แทนได้

### 5.2 map() ใช้ทำอะไร

ใช้แปลงข้อมูลแต่ละรายการเป็นค่าใหม่ เช่น HTML  
forEach ใช้แทนได้แต่ map เหมาะกว่า

---

## ส่วนที่ 6: ข้อบกพร่องและการแก้ไข

### 6.1 เริ่มหลาย task พร้อมกัน

อาจมี timer หลายตัวทำงานพร้อมกัน  
ทำให้ระบบทำงานหนักและเวลาไม่ตรง

### 6.2 รีเฟรชแล้วข้อมูลหาย

เพราะข้อมูลเก็บใน memory  
ควรเก็บใน LocalStorage

### 6.3 ทดสอบ timer 30 นาที

ใช้เวลานานเกินไป  
ควรลดเวลาเพื่อทดสอบ

---

## ส่วนที่ 7: Enhancement

### 7.1 การรีเซ็ต timer

ต้องหยุด timer ก่อน แล้วตั้งค่าเวลาใหม่เป็นค่าเริ่มต้น

### 7.2 เก็บข้อมูลใน LocalStorage

ใช้ JSON.stringify() บันทึก  
ใช้ JSON.parse() โหลดกลับ

### 7.3 ให้ผู้ใช้กำหนดเวลาเอง

เพิ่ม input รับค่าเวลา แล้วนำไปใช้แทนค่าเดิม

---

## ส่วนที่ 8: Concepts เชิงลึก

### 8.1 ทำไมต้องเรียก render หลายครั้ง

เพราะ UI ไม่เปลี่ยนเองเมื่อข้อมูลเปลี่ยน  
ควรมีจุดควบคุมการอัปเดตกลาง

### 8.2 requestAnimationFrame vs setInterval

- requestAnimationFrame เหมาะกับ animation
- setInterval เหมาะกับ timer

### 8.3 Memory Leak จาก setInterval

อาจเกิดหากไม่ clearInterval  
ควรหยุดทุกครั้งเมื่อไม่ใช้งาน

---

# Pomodoro Timer

## ส่วนที่ 1: เทคนิค Pomodoro

### 1.1 Pomodoro คืออะไร

เทคนิคบริหารเวลาแบบทำงาน 25 นาที พัก 5 นาที  
ช่วยให้โฟกัสดีและลดความล้า

### 1.2 การปรับเวลา Work/Break

แก้ค่าที่กำหนด duration ในโค้ด  
เช่น workDuration หรือ breakDuration

### 1.3กin 1.3 Long Break หลัง 4 รอบ

ช่วยให้สมองพักจริง ลดความล้าสะสม

---

## ส่วนที่ 2: State

### 2.1 State ที่ใช้

- timeLeft
- phase
- rounds
- isRunning
- intervalId

### 2.2 ทำไมต้องมี rounds

เพื่อรู้จำนวนรอบที่ทำงาน  
ใช้ตัดสินใจ Long Break

### 2.3 อัปเดต count เมื่อจบ work

เพิ่มค่า counter ในฟังก์ชัน completePhase()

---

## ส่วนที่ 3: Timer Logic

### 3.1 setInterval ทำงานอย่างไร

ทุกวินาทีจะ:

- ลด timeLeft
- อัปเดตหน้าจอ
- ตรวจสอบว่าหมดเวลาหรือยัง

### 3.2 ทำไมต้องมี timerInterval

เพื่อใช้หยุด timer ด้วย clearInterval

### 3.3 กด Start ซ้ำ

อาจเกิด interval ซ้อน  
ควรตรวจสอบสถานะก่อนเริ่ม

---

## ส่วนที่ 4: DOM & UI

### 4.1 updateDisplay() ทำอะไร

แสดงเวลา phase และ progress bar  
ต้องเรียกเมื่อค่าเวลาเปลี่ยน

### 4.2 padStart ใช้ทำอะไร

เติมเลข 0 ด้านหน้าให้ครบสองหลัก

### 4.3 สูตร progress

เปอร์เซ็นต์ = (เวลาที่ผ่าน / เวลาทั้งหมด) × 100

---

## ส่วนที่ 5: Controls

### 5.1 ปุ่มควบคุม

- Start/Pause → เริ่มหรือหยุด
- Reset → กลับค่าเริ่มต้น

### 5.2 ทำไม Pause ต้อง clearInterval

เพื่อหยุด timer จริง ๆ

### 5.3 Reset ต้องรีเซ็ตอะไร

- เวลา
- phase
- rounds
- สถานะการทำงาน

---

## ส่วนที่ 6: Notifications & Sound

### 6.1 playSound ใช้อะไร

อาจใช้ Web Audio API หรือ HTML Audio

### 6.2 ทำไมเสียงต่างกัน

เพื่อให้รู้ว่าเข้าสู่ช่วงไหน

### 6.3 Desktop Notification

ใช้ Notification API พร้อมขอ permission

---

## ส่วนที่ 7: Stats

### 7.1 ข้อมูลที่ต้องเก็บ

- จำนวนรอบที่เสร็จ
- เวลารวมที่ทำงาน
- จำนวนพักยาว

### 7.2 จุดอัปเดต stats

เมื่อจบแต่ละ phase

### 7.3 เก็บใน LocalStorage

บันทึกเป็น JSON แล้วโหลดกลับ

---

## ส่วนที่ 8: Bug Detection

### 8.1 เปิด DevTools timer ยังเดินไหม

ยังเดิน แต่ความแม่นยำอาจลดลง

### 8.2 completePhase ขณะ timer ยังวิ่ง

อาจเกิด interval ซ้อน

### 8.3 ล็อกหน้าจอมือถือ

timer อาจหยุด  
ควรใช้ timestamp คำนวณเวลา

---

# Weather App (Fetch API)

## ส่วนที่ 1: Fetch API

### 1.1 Fetch API คืออะไร

ใช้ส่ง request ไปยัง server และรับข้อมูลกลับ  
ง่ายกว่า XMLHttpRequest

### 1.2 การทำงานของโค้ด fetch

ส่ง request → รับ response → แปลง JSON → ใช้ข้อมูล → จัดการ error

### 1.3 Network error

จะถูกจับใน catch

---

## ส่วนที่ 2: Geocoding API

### 2.1 หน้าที่

แปลงชื่อเมืองเป็นพิกัด latitude และ longitude

### 2.2 ค้นหา Bangkok

ต้องเรียก Geocoding API ก่อนเพื่อรับพิกัด

### 2.3 เมืองชื่อซ้ำ

อาจเลือกผลลัพธ์แรก หรือให้ผู้ใช้เลือก

---

## ส่วนที่ 3: Weather Data

### 3.1 Parameters ที่ใช้

latitude, longitude และข้อมูลที่ต้องการ

### 3.2 weather_code ใช้ทำอะไร

ใช้บอกประเภทสภาพอากาศเพื่อนำไปแสดง icon

### 3.3 current vs hourly

- current → สภาพอากาศปัจจุบัน
- hourly → พยากรณ์รายชั่วโมง

---

## ส่วนที่ 4: Async/Await

### 4.1 ข้อดี

อ่านง่ายเหมือนโค้ดปกติ  
จัดการ error ง่าย

### 4.2 await ทำอะไร

รอให้ Promise เสร็จก่อนทำคำสั่งต่อไป

### 4.3 try-catch

ใช้ดักจับ error ระหว่างทำงาน

---

## ส่วนที่ 5: DOM

### 5.1 ต้องแสดงอะไรบ้าง

ชื่อเมือง อุณหภูมิ ความชื้น และ icon

### 5.2 Loading spinner

แสดงขณะโหลดข้อมูล และซ่อนเมื่อเสร็จ

### 5.3 อัปเดตทุก 10 นาที

ใช้ setInterval

---

## ส่วนที่ 6: LocalStorage

### 6.1 ใช้เพื่ออะไร

เก็บข้อมูลถาวรใน browser

### 6.2 ทำไมต้อง JSON.stringify

เพราะ LocalStorage เก็บได้เฉพาะ string

### 6.3 โหลดข้อมูลเมื่อ refresh

ดึงจาก LocalStorage แล้วแปลงกลับ

---

## ส่วนที่ 7: Error Handling

### 7.1 เมืองไม่พบ

แสดงข้อความแจ้งเตือน

### 7.2 Offline

fetch จะเกิด error

### 7.3 กด recent city

โหลดข้อมูลของเมืองนั้นใหม่

---

## ส่วนที่ 8: Weather Display

### 8.1 เลือก icon

ใช้ mapping ระหว่าง weather code กับ icon

### 8.2 ความหมายค่าต่าง ๆ

- อุณหภูมิ → ความร้อน
- ความชื้น → ปริมาณไอน้ำ
- ลม → ความเร็วลม

### 8.3 Forecast 24 ชั่วโมง

ใช้ข้อมูลใน array hourly

---

## ส่วนที่ 9: Responsive Design

### 9.1 Layout บนมือถือ

ควรเป็นแนวตั้งเพื่ออ่านง่าย

### 9.2 เมื่อหมุนหน้าจอ

ปรับ layout ตามขนาดหน้าจอ

### 9.3 ขนาดตัวอักษร

อย่างน้อย 16px เพื่อแตะง่าย

---

## ส่วนที่ 10: Architecture & Enhancement

### 10.1 Weather class

สร้าง class สำหรับจัดการ logic และ API

### 10.2 Favorite Cities

เพิ่มปุ่มและเก็บข้อมูลใน LocalStorage

### 10.3 Dark Mode

ใช้ CSS variables และสลับ class ด้วย JavaScript
