# Answer The Questions Lab09

## Todo List App

## ส่วนที่ 1: การทำความเข้าใจโค้ด

### คำถาม 1.1 เหตุใดจึงต้องแยก duration และ timeLeft ออกจากกัน อธิบายความแตกต่าง

- duration คือค่าระยะเวลาที่กำหนดไว้ตั้งแต่เริ่มต้น
- timeLeft คือเวลาที่เหลือจริงระหว่างการนับถอยหลัง
- การแยกตัวแปรช่วยให้สามารถรีเซ็ตค่าเดิมหรือใช้คำนวณความคืบหน้าได้โดยไม่สูญเสียข้อมูลตั้งต้น

### คำถาม 1.2 formatTime() ฟังก์ชันนี้ทำความสำคัญอะไร? ถ้าไม่มีฟังก์ชันนี้ app จะเป็นยังไง

- ใช้แปลงค่าที่เป็นวินาทีให้แสดงในรูปแบบ นาที:วินาที
- หากไม่มี ผู้ใช้จะเห็นตัวเลขวินาทีอย่างเดียว ทำให้ดูไม่เป็นระเบียบและอ่านยาก

### คำถาม 1.3 อธิบายข้อแตกต่างระหว่าง setInterval() และ setTimeout()

- setInterval() เรียกฟังก์ชันซ้ำตามช่วงเวลาที่กำหนด
- setTimeout() เรียกฟังก์ชันเพียงครั้งเดียว
- งานที่ต้องทำซ้ำต่อเนื่อง เช่น timer จึงเหมาะกับ setInterval()

---

## ส่วนที่ 2: ฟังก์ชันการทำงาน

### คำถาม 2.1 เมื่อผู้ใช้คลิกปุ่ม “▶️ เริ่ม” ครั้งแรก เกิดอะไรขึ้นในลำดับใด (ระบุขั้นตอนอย่างน้อย 4 ขั้น)

1. ตรวจสอบสถานะว่า task กำลังทำงานหรือไม่
2. เปลี่ยนสถานะเป็นกำลังทำงาน
3. สร้าง interval สำหรับนับเวลา
4. ลดค่าเวลาที่เหลือทุกวินาที
5. อัปเดตการแสดงผลบนหน้าจอ

### คำถาม 2.2 ในฟังก์ชัน toggleTimer() ทำไมจึงต้องมี clearInterval() ก่อนแล้วจึงตั้ง task.isRunning = false

- เพื่อหยุดตัวจับเวลาให้หยุดจริงก่อนเปลี่ยนสถานะ
- หากไม่ทำ อาจมี interval ทำงานซ้อนกัน

### คำถาม 2.3 ถ้า timer หมดเวลา จะเกิดความผิดพลาดใดหากไม่มี clearInterval()

- เวลาจะยังคงลดต่อไป
- อาจเกิดการเรียกฟังก์ชันซ้ำ
- ทำให้ระบบทำงานผิดปกติ

---

## ส่วนที่ 3: DOM และ UI

### คำถาม 3.1 ฟังก์ชัน renderTasks() สามารถแยกออกเป็นฟังก์ชันย่อยได้กี่ฟังก์ชัน จงเสนอการแยกที่เหมาะสม

สามารถแบ่งเป็นส่วน ๆ เช่น

- ฟังก์ชันสร้าง element ของ task
- ฟังก์ชันกำหนดรูปแบบตามสถานะ
- ฟังก์ชันจัดการกรณีไม่มีข้อมูล

### คำถาม 3.2 ใช้วิธีใดในการอัปเดต UI ทุกครั้งที่ timer เปลี่ยนแปลง มีวิธีการอื่นนอกเหนือจำนวนปัจจุบันหรือไม่

- วิธีหลักคือ render ใหม่เมื่อข้อมูลเปลี่ยน
- อีกวิธีคือปรับเฉพาะ element ที่เปลี่ยนเพื่อลดภาระระบบ

### คำถาม 3.3 “keypress” event listener ทำความสำคัญอะไร หากลบออกไป app จะเป็นยังไง

- ช่วยให้ควบคุมแอปด้วยคีย์บอร์ด
- หากไม่มี ผู้ใช้ยังใช้งานได้แต่ต้องใช้เมาส์เท่านั้น

---

## ส่วนที่ 4: Web Audio API

### คำถาม 4.1 อธิบายหน้าที่ของการตั้งค่า:

1. oscillator.frequency.value = 800

- กำหนดระดับความสูงของเสียง

2. oscillator.type = "sine"

- กำหนดรูปแบบคลื่นเสียง

3. gainNode.gain.setValueAtTime(0.3, audioContext.currentTime)

- กำหนดระดับความดัง

### คำถาม 4.2 ทำไมเสียง alarm จึงใช้เวลา 0.5 วินาที แล้วหยุด ถ้าเพิ่มเป็น 2 วินาทีจะส่งผลต่อ UX อย่างไร

- เสียงสั้นช่วยเตือนโดยไม่รบกวน
- หากยาวเกินไปอาจทำให้ผู้ใช้รู้สึกไม่สบายหรือเสียสมาธิ

---

## ส่วนที่ 5: Array Methods

### คำถาม 5.1 ในฟังก์ชัน deleteTask(id) ใช้ filter() เพื่ออะไร เขียนวิธีอื่นแทน filter() ได้หรือไม่

- ใช้สร้างรายการใหม่โดยตัด task ที่ต้องการลบออก
- สามารถใช้ findIndex() แล้วลบด้วย splice() ได้

### คำถาม 5.2 ในฟังก์ชัน renderTasks() ใช้ map() เพื่อทำอะไร ถ้าใช้ forEach() แทนจะดีหรือไม่

- ใช้แปลงข้อมูล task ให้เป็น HTML
- forEach() ใช้แทนได้แต่ต้องจัดการตัวแปรเพิ่มเติม

---

## ส่วนที่ 6: ข้อบกพร่องและการแก้ไข

### คำถาม 6.1 หากผู้ใช้เพิ่ม task 2 อัน แล้วคลิก “เริ่ม” บน task ทั้ง 2 อันพร้อมๆ กัน จะเกิดอะไรขึ้น

- timer จะทำงานพร้อมกันทั้งสองรายการ
- อาจทำให้ระบบต้องอัปเดตบ่อยขึ้น

### คำถาม 6.2 ถ้า timer กำลังเดินอยู่ และผู้ใช้รีเฟรชหน้า (F5) task จะหายไป ทำไม วิธีแก้ปัญหาคือ

- เพราะข้อมูลอยู่ใน memory ชั่วคราว
- ควรบันทึกลง LocalStorage

### คำถาม 6.3 ในการทดสอบ app ที่ timer 30 นาที จะใช้เวลานานไปหรือไม่ เสนอวิธีทดสอบที่เร็วขึ้น

- ใช้เวลานานเกินไป
- ควรตั้งเวลาให้สั้นลงเพื่อทดสอบ

---

## ส่วนที่ 7: Enhancement

### คำถาม 7.1 หากต้องการเพิ่มฟังก์ชัน “รีเซ็ต timer” คืนไปเป็น 30 นาที โค้ดจะเป็นยังไง

- หยุด timer ที่กำลังทำงาน
- ตั้งค่าเวลาใหม่เป็นค่าเริ่มต้น
- อัปเดตหน้าจอ

### คำถาม 7.2 วิธีใดในการเก็บ tasks ไว้ใน LocalStorage เพื่อไม่ให้หายเมื่อรีเฟรชหน้า

- บันทึกข้อมูลเป็น JSON ลง LocalStorage
- โหลดกลับมาเมื่อเปิดหน้าใหม่

### คำถาม 7.3 ถ้าต้องการให้ผู้ใช้กำหนดเวลา timer เอง จะต้องแก้ไขฟังก์ชันไหนบ้าง

- เพิ่ม input สำหรับรับเวลา
- ปรับฟังก์ชันสร้าง task ให้ใช้ค่าจาก input

---

## ส่วนที่ 8: Concepts เชิงลึก

### คำถาม 8.1 ทำไม renderTasks() จึงต้องเรียกหลายครั้ง มีวิธี DRY ที่ดีกว่าหรือไม่

- UI ต้องอัปเดตทุกครั้งที่ข้อมูลเปลี่ยน
- ควรแยกการอัปเดตเฉพาะส่วนที่จำเป็น

### คำถาม 8.2 ถ้าเปลี่ยนจาก setInterval เป็น requestAnimationFrame จะส่งผลดีหรือไม่

- เหมาะกับ animation มากกว่า
- สำหรับ timer ปกติ setInterval ใช้ง่ายกว่า

### คำถาม 8.3 ในโค้ดที่ใช้ setInterval() มี Memory Leak ที่อาจเกิดขึ้นได้หรือไม่

- อาจเกิดหากไม่หยุด interval ก่อนลบ task
- ทำให้ยังมี process ทำงานอยู่เบื้องหลัง

---

# Pomodoro Timer

## ส่วนที่ 1: ความเข้าใจเทคนิค Pomodoro

### คำถาม 1.1 เทคนิค Pomodoro คืออะไร เหตุใดจึงเลือกใช้เวลา 25 นาที สำหรับการทำงาน

- เป็นเทคนิคแบ่งเวลาทำงานเป็นช่วงสั้น ๆ สลับกับพัก
- 25 นาทีเป็นช่วงที่ช่วยให้โฟกัสได้ดีโดยไม่เหนื่อยเกินไป

### คำถาม 1.2 หากต้องการปรับเวลา Work/Break ให้แตกต่างจาก default (25/5) จะต้องแก้ไขโค้ดส่วนไหน

- ปรับค่าระยะเวลาในตัวแปรที่กำหนดเวลา

### คำถาม 1.3 ทำไมหลังจาก 4 รอบ (pomodoros) จึงมี Long Break ขนาด 15 นาที ประโยชน์คืออะไร

- เพื่อพักฟื้นสมองหลังทำงานต่อเนื่อง
- ช่วยลดความเหนื่อยล้า

---

## ส่วนที่ 2: การจัดโครงสร้าง State

### คำถาม 2.1 ปัจจุบัน app เก็บ state อะไรบ้าง

- เวลาที่เหลือ
- สถานะการทำงาน
- phase ปัจจุบัน
- จำนวนรอบ

### คำถาม 2.2 ทำไมจึงต้องมี rounds state หากแค่เก็บ phase ก็พอ

- เพื่อรู้จำนวนรอบที่ทำไป
- ใช้ตัดสินใจว่าจะพักยาวเมื่อใด

### คำถาม 2.3 ถ้าต้องการให้ Pomodoro Count อัปเดต เมื่อจบแต่ละ Work phase จะแก้ไขฟังก์ชัน completePhase() ยังไง

- เพิ่มการเพิ่มค่าตัวนับเมื่อ phase เป็น work

## ส่วนที่ 3: Timer Logic

### คำถาม 3.1 อธิบายการทำงานของ setInterval() ในส่วน timer - จะเกิดอะไรขึ้นทุก 1 วินาที

1.  ลดค่า `state.timeLeft` ลง 1
2.  ตรวจสอบว่า `timeLeft <= 0` หรือไม่ (ถ้าใช่ คือหมดเวลา ให้หยุด timer และสลับ phase)
3.  เรียก `updateDisplay()` เพื่อวาดหน้าจอใหม่ (อัปเดตตัวเลขเวลา และ Progress Bar)

### คำถาม 3.2 เหตุใดจึงต้องมี timerInterval เป็น global variable ถ้าไม่มี app จะเป็นยังไง

- สาเหตุหลักคือ "ฟังก์ชันที่สั่งเริ่ม" กับ "ฟังก์ชันที่สั่งหยุด" มันเป็นคนละคนกัน
  **ถ้าไม่มี (ไม่เก็บ ID ไว้) แอปจะเป็นยังไง?**
- นาฬิกาผีสิง (Zombie Timer)
- อาการ "เวลาเบิ้ล" (Multiple Intervals)
- Memory Leak

### คำถาม 3.3 ถ้าผู้ใช้คลิก “เริ่ม” 2 ครั้งติดต่อกัน จะเกิดอะไรขึ้น มีวิธีแก้หรือไม่

- ในโค้ดปัจจุบัน (ฟังก์ชัน `startTimer`) มีการเช็ค `if (state.isRunning)` อยู่แล้ว
  - **ครั้งที่ 1:** `state.isRunning` เป็น false -> เข้า Loop `else` -> เริ่ม Timer
  - **ครั้งที่ 2:** `state.isRunning` เป็น true -> เข้า Loop `if` -> หยุด Timer (Pause)
  - **สรุป:** ปุ่มทำหน้าที่เป็น Toggle (Start/Pause) อยู่แล้ว จึงไม่มีปัญหาเรื่องกดซ้ำแล้ว Timer วิ่งซ้อนกัน

## ส่วนที่ 4: DOM & UI

### คำถาม 4.1 ฟังก์ชัน updateDisplay() ทำอะไร ต้องเรียกจากที่ไหนบ้าง

- การคำนวณ (Math) แปลงค่าวินาทีทั้งหมดให้กลายเป็น นาที และ วินาที, การจัดรูปแบบ, การแสดงผล
- **เรียกจาก:**
  1.  ทุกๆ 1 วินาที ใน `setInterval`
  2.  ตอนกดปุ่ม Start/Pause/Reset
  3.  ตอนเปลี่ยนค่า Setting (Input Change)
  4.  ตอนโหลดหน้าเว็บครั้งแรก

### คำถาม 4.2 formatTime() ใช้ padding ด้วย padStart(2, "0") เพื่ออะไร ถ้าไม่มี timer จะแสดง 5:3 แทน 05:03 ได้หรือไม่

- เพื่อ "รักษาความสวยงามและมาตรฐานของการแสดงผลเวลา"
- ถ้าไม่มี 5 นาที 3 วินาที จะแสดงเป็น "5:3" ซึ่งดูไม่เป็นมืออาชีพและอ่านยาก
- มี `padStart` จะแสดงเป็น "05:03"

### คำถาม 4.3 Progress bar คำนวณเปอร์เซ็นต์ยังไง เขียนสูตรออกมา

- **คำตอบ:**
  $$ \text{Progress \%} = \left( \frac{\text{Total Time} - \text{Time Left}}{\text{Total Time}} \right) \times 100 $$
- โค้ด: `const progress = ((state.totalTime - state.timeLeft) / state.totalTime) * 100;`

## ส่วนที่ 5: Events & Controls

### คำถาม 5.1 ปุ่มควบคุมมีอะไรบ้าง (Start, Pause, Reset, Settings) แต่ละปุ่มเรียกฟังก์ชันไหน

- **Start/Pause Button** เรียก `startTimer()` (ซึ่งทำหน้าที่ Toggle Start/Stop)
- **Reset Button** เรียก `resetTimer()`
- **Settings Input** เรียก Event Listener `change` เพื่ออัปเดต `state.workDuration`/`state.breakDuration`

### คำถาม 5.2 ทำไม Pause ต้องหยุด timer ด้วย clearInterval() หากไม่ทำจะไปไป

- "หยุดเครื่องยนต์" ของตัวนับเวลาได้สนิท
  **หากไม่ทำ**
- เวลาจะไม่ยอมหยุด
- เกิดอาการ "นาฬิกาผีสิง"
- เปลืองทรัพยากร

### คำถาม 5.3 Reset button ควรตั้งค่ากลับเป็นอะไร:timeLeft, phase, rounds, timerInterval

- `state.isRunning = false` (หยุดเดิน)
- `state.isWorkPhase = true` (กลับมาเริ่มที่โหมดทำงาน)
- `state.currentSession = 1` (เริ่มนับรอบใหม่)
- `state.timeLeft` = ค่าเริ่มต้นของ Work Duration
- **ไม่จำเป็นต้อง** ลบ Stats (Completed Sessions) เว้นแต่จะเป็น Hard Reset

## ส่วนที่ 6: Notifications & Sound

### คำถาม 6.1 ฟังก์ชัน playSound() ใช้เทคนิคอะไรในการสร้างเสียง (Web Audio API? HTML Audio?)

- HTML Audio Element (วิธีที่ง่ายที่สุด)
- Web Audio API (วิธีระดับโปร)

### คำถาม 6.2 ทำไม notification ควร work/break ต่างกันอย่างไร เสียง alarm ที่ดีควรเป็นยังไง

- **ความต่าง** เพื่อให้ผู้ใช้รู้ได้ทันทีด้วยการฟัง (Auditory Cue) ว่าจบช่วงไหนแล้ว เช่น เสียงจบ Work อาจจะสั้นกระชับ (เตือนให้หยุด) เสียงจบ Break อาจจะยาวกว่าหรือตื่นตัวกว่า (เตือนให้กลับมาทำงาน)
- **เสียงที่ดี** ชัดเจนแต่ไม่ตกใจ (Startle) และไม่ควรยาวเกินไปจนน่ารำคาญ

### คำถาม 6.3 ถ้าต้องการเพิ่ม Desktop Notification ด้วย Browser Notification API จะแก้ไขฟังก์ชัน completePhase() ยังไง

1.  ขอ Permission ก่อนด้วย `Notification.requestPermission()`
2.  ในจังหวะหมดเวลา ให้เรียก:
    ```javascript
    if (Notification.permission === "granted") {
      new Notification("หมดเวลา!", {
        body: state.isWorkPhase ? "พักผ่อนได้แล้ว" : "กลับมาทำงานต่อได้แล้ว",
        icon: "icon.png",
      });
    }
    ```

## ส่วนที่ 7: Progress Tracking & Stats

### คำถาม 7.1 Stats ต้องติดตามข้อมูลอะไร:Pomodoros ที่เสร็จแล้ว, เวลารวมที่ทำงาน, Long breaks ที่เกิดขึ้น

1.  **Pomodoros:** จำนวนรอบที่ทำสำเร็จ (Completed Sessions)
2.  **Total Focus Time:** เวลารวมทั้งหมดที่ใช้ในการทำงาน (Completed Sessions \* Work Duration) ซึ่งในโค้ดมีคำนวณไว้แล้ว

### คำถาม 7.2 ตำแหน่งที่ดีที่สุดในการอัปเดต stats คือที่ไหน

- ใน Block เงื่อนไข **"เมื่อหมดเวลา" (`if (state.timeLeft <= 0)`)** และ **"เป็นช่วง Work Phase"**
- เพราะเป็นจุดเดียวที่ยืนยันได้ว่าผู้ใช้ทำงานจนจบครบเวลาจริงๆ ไม่ใช่กด Reset กลางคัน

### คำถาม 7.3 วิธีการบันทึก stats ไว้ใน LocalStorage ได้อย่างไร

```javascript
// บันทึก
localStorage.setItem(
  "pomodoroStats",
  JSON.stringify({
    completed: state.completedSessions,
  }),
);

// โหลดตอนเปิดเว็บ
const saved = JSON.parse(localStorage.getItem("pomodoroStats"));
if (saved) state.completedSessions = saved.completed;
```

## ส่วนที่ 8: Bug Detection

### คำถาม 8.1 ถ้า timer กำลังเดินอยู่ แล้วผู้ใช้เปิด DevTools ขนาดใหญ่ timer จะยังคงเดินต่อหรือไม่

- เดินต่อแต่มีโอกาสที่จะเกิดอาการ "เดินช้าลง" หรือ "กระตุก" ได้ ขึ้นอยู่กับว่าทรัพยากรเครื่องในขณะนั้นถูกใช้งานหนักแค่ไหน

### คำถาม 8.2 ถ้า completePhase() เรียก ในขณะที่ timer ยังวิ่งอยู่ จะเกิด memory leak ได้หรือไม่

- มีโอกาสเกิดขึ้นได้สูง แต่ที่น่ากลัวกว่า Memory Leak คืออาการ "นาฬิกาซ้อนร่าง" ที่จะทำให้แอปทำงานผิดเพี้ยนไปหมด

### คำถาม 8.3 ใน mobile ถ้าผู้ใช้ล็อก screen ขณะ timer เดิน timer จะหยุดหรือยังคงเดิน วิธีแก้คืออะไร

- **ปัญหา:** Browser ในมือถือมักจะหยุด Pause JavaScript execution เมื่อปิดหน้าจอ ทำให้เวลานับจังหวะ `setInterval` หยุดทำงาน เมื่อเปิดมาเวลาจะเท่าเดิม (ไม่ลดลงตามจริง)
- **วิธีแก้:** แทนที่จะนับถอยหลังทีละ 1, ให้บันทึก **Timestamp เป้าหมาย (EndTime)** ไว้ตอนเริ่ม Start
  - `endTime = Date.now() + (state.timeLeft * 1000)`
  - ใน Loop ให้คำนวณหาความต่าง: `timeLeft = Math.round((endTime - Date.now()) / 1000)`
  - วิธีนี้ต่อให้ปิดหน้าจอ เวลาจะยังคงถูกคำนวณตามเวลาจริงของโลก (Wall Clock Time)

## ส่วนที่ 9: Architecture & Code Quality

### คำถาม 9.1 ถ้าต้องการแยก Timer logic ออกจาก UI ควรสร้าง class อย่างไร

- สร้าง Class `PomodoroTimer` ที่จัดการเฉพาะ Logic ตัวเลขและ State โดยมี Methods: `start()`, `pause()`, `reset()`, `tick()` และใช้ Callback หรือ Custom Event เพื่อส่งค่า `timeLeft` กลับมาให้ UI อัปเดต

### คำถาม 9.2 Functions ที่มีมากควรจัดไว้ใน object เพื่อจัดการได้ดีขึ้นหรือไม่

- การจัดกลุ่มฟังก์ชัน (Functions) ที่มีความเกี่ยวข้องกันไว้ใน Object เป็นหนึ่งในแนวคิดพื้นฐานของ Encapsulation ซึ่งช่วยให้โค้ดดูเป็นมืออาชีพและจัดการได้ง่ายขึ้นมาก

### คำถาม 9.3 Code reusability - ส่วนไหนของโค้ดสามารถทำให้เป็น utility functions ได้

1.  `formatTime(seconds)`: ใช้แปลงเวลาได้ทุกโปรแกรม
2.  `playSound(freq, type)`: รับพารามิเตอร์ความถี่และประเภทเสียง เพื่อใช้ซ้ำในโปรแกรมอื่น
3.  `padZero(num)`: ฟังก์ชันเติมเลข 0 หน้าหลัก

## ส่วนที่ 10: Enhancements

### คำถาม 10.1 วิธีเพิ่ม Dark Mode ควรแก้ไข CSS/JS ส่วนไหนบ้าง

- **JS:** เพิ่มปุ่ม Toggle, สั่ง `document.body.classList.toggle('dark-mode')`
- **CSS:** กำหนด Variable สี (CSS Custom Properties) ใน `:root` และ override ใน `.dark-mode`
  ```css
  :root {
    --bg-color: #eee;
    --text-color: #333;
  }
  .dark-mode {
    --bg-color: #222;
    --text-color: #fff;
  }
  body {
    background: var(--bg-color);
    color: var(--text-color);
  }
  ```

### คำถาม 10.2 วิธีบันทึกประวัติการใช้งาน (session history) คืออะไร

- การบันทึกประวัติการใช้งาน (Session History) เพื่อให้ข้อมูลไม่หายไปเมื่อปิดเบราว์เซอร์หรือรีเฟรชหน้าเว็บ วิธีที่ได้รับความนิยมและเหมาะสมที่สุดสำหรับแอปขนาดเล็กถึงกลางคือการใช้ localStorage

### คำถาม 10.3 วิธีเพิ่ม Keyboard Shortcuts (Space = Start/Pause, R = Reset) ควรใช้ event ไหน

- ใช้ `document.addEventListener('keydown', (e) => { ... })`
  - ตรวจสอบ `e.code`:
  - `if (e.code === 'Space') startBtn.click();`
  - `if (e.key === 'r') resetBtn.click();`

---

# Weather App (Fetch API)

## ส่วนที่ 1: Fetch API & HTTP Requests

### คำถาม 1.1 Fetch API คืออะไร ความแตกต่างระหว่าง Fetch API และ XMLHttpRequest คืออะไร

- Fetch API ใช้ส่ง request ไปยัง server และรับข้อมูลกลับแบบ asynchronous
- เขียนง่ายกว่า XMLHttpRequest ซึ่งมีโครงสร้างซับซ้อนกว่า

### คำถาม 1.2 โค้ดนี้ทำอะไร อธิบายการทำงานแต่ละขั้นตอน

- ส่ง request ไปยัง URL
- รับ response
- แปลงเป็น JSON
- ใช้ข้อมูล
- จัดการ error หากเกิดขึ้น

### คำถาม 1.3 ถ้า API response ล้มเหลว (network error) จะถูก handle ที่ไหน .catch() จะจับได้หรือไม่

- network error จะถูกจับใน catch

---

## ส่วนที่ 2: Geocoding API

### คำถาม 2.1 Geocoding API ทำหน้าที่อะไร ทำไมจึงต้องใช้ก่อนเรียก Weather API

- แปลงชื่อเมืองเป็นพิกัด
- Weather API ต้องใช้พิกัดในการค้นหาข้อมูล

### คำถาม 2.2 When user searches “Bangkok” ควรส่ง request ไปที่ endpoint ไหนก่อน ได้ข้อมูลอะไรจาก response

- เรียก Geocoding API เพื่อรับ latitude และ longitude

### คำถาม 2.3 ถ้า user ค้นหาเมือง “Nonthaburi” ซึ่งชื่อเดียวกับเมืองอื่น app จะตัดสินใจอย่างไร

- อาจเลือกผลลัพธ์แรก หรือให้ผู้ใช้เลือก

---

## ส่วนที่ 3: Weather Data & Parameters

### คำถาม 3.1 Weather API ต้องการ parameters อะไรบ้าง

- latitude
- longitude
- ข้อมูลสภาพอากาศที่ต้องการ

### คำถาม 3.2 Parameters นี้ request ข้อมูลอะไร ทำไมต้องใช้ weather_code

- ใช้ดึงข้อมูลสภาพอากาศ
- weather_code ใช้ระบุประเภทอากาศเพื่อนำไปแสดง icon

### คำถาม 3.3 ความแตกต่างระหว่าง current weather และ hourly forecast คืออะไร

- current คือข้อมูลปัจจุบัน
- hourly คือข้อมูลล่วงหน้าเป็นรายชั่วโมง

---

## ส่วนที่ 4: Async/Await Pattern

### คำถาม 4.1 ทำไมจึงใช้ async/await แทน .then().catch() มีข้อดีอะไรบ้าง

- โค้ดอ่านง่าย
- จัดการ error ได้สะดวก

### คำถาม 4.2 await ทำอะไร ถ้าไม่ใช้ code จะเป็นยังไง

- รอให้ Promise เสร็จก่อนทำคำสั่งถัดไป
- หากไม่ใช้ อาจได้ค่าที่ไม่สมบูรณ์

### คำถาม 4.3 try-catch block ใช้เพื่ออะไร ตกลง error ที่ catch ได้อะไร

- ใช้ดักจับข้อผิดพลาดระหว่างการทำงาน

---

## ส่วนที่ 5: DOM Manipulation

### คำถาม 5.1 เมื่อได้ข้อมูล weather มา app ต้องแสดงผล element ใดบ้าง

- ชื่อเมือง
- อุณหภูมิ
- สภาพอากาศ
- ความชื้น

### คำถาม 5.2 Loading spinner ปรากฏในไหน เมื่อไหร่ควรแสดง/ซ่อน

- แสดงระหว่างโหลดข้อมูล
- ซ่อนเมื่อโหลดเสร็จ

### คำถาม 5.3 ถ้าต้องอัปเดต temperature display ทุก 10 นาที ควรใช้ setInterval() หรือ setTimeout()

- ใช้ setInterval() เพราะต้องทำซ้ำ

---

## ส่วนที่ 6: Data Storage (LocalStorage)

### คำถาม 6.1 LocalStorage ใช้เพื่ออะไร บันทึก recent cities ยังไง

- ใช้เก็บข้อมูลถาวรใน browser
- บันทึกด้วย setItem

### คำถาม 6.2 ทำไมต้องใช้ JSON.stringify() ถ้าเก็บ object โดยตรงจะเป็นอย่างไร

- เพราะ LocalStorage เก็บได้เฉพาะ string
- หากเก็บ object ตรง ๆ จะไม่สามารถนำกลับมาใช้ได้

### คำถาม 6.3 เมื่อ refresh หน้า app ควร restore recent cities จากไหน โค้ดเป็นยังไง

- โหลดจาก LocalStorage แล้วแปลงกลับ

---

## ส่วนที่ 7: Error Handling & Edge Cases

### คำถาม 7.1 ถ้า user ค้นหาเมืองที่ไม่มี (เช่น “XYZ”) app จะแสดง error อย่างไร

- แสดงข้อความว่าไม่พบข้อมูล

### คำถาม 7.2 ถ้า network ไม่มี (offline) fetch จะ throw error หรือ return success

- จะเกิด error

### คำถาม 7.3 เมื่อ hover หรือ click ที่ recent city button โปรแกรมจะทำอะไร

- โหลดข้อมูลของเมืองนั้นใหม่

---

## ส่วนที่ 8: Weather Display

### คำถาม 8.1 Weather code 0 = Clear sky, 1 = Partly cloudy, 80 = Drizzle… เลือก icon/emoji ยังไง

- ใช้ mapping ระหว่าง code กับ icon

### คำถาม 8.2 ความแตกต่างระหว่าง อุณหภูมิ (°C), ความชื้น (%), ความเร็วลม (m/s) คืออะไร

- แต่ละค่าบอกสภาพอากาศคนละด้าน

### คำถาม 8.3 Hourly forecast แสดง 24 ชั่วโมง ต้องเลือก element ไหนจาก response

- ใช้ข้อมูลในส่วน hourly ของ response

---

## ส่วนที่ 9: Responsive Design & Mobile

### คำถาม 9.1 ใน mobile screen weather card ควร มี layout อย่างไร (vertical/horizontal)

- ควรเป็นแนวตั้งเพื่ออ่านง่าย

### คำถาม 9.2 เมื่อผู้ใช้ rotate device grid/flex ควรตัดสินใจอย่างไร

- ปรับ layout ตามความกว้างหน้าจอ

### คำถาม 9.3 Input field และ button บน mobile ต้องมี font size เท่าไร (ควรมากพอให้แตะง่าย)

- อย่างน้อยประมาณ 16px

---

## ส่วนที่ 10: Code Architecture & Enhancement

### คำถาม 10.1 ถ้าต้องสร้าง Weather class เพื่อจัดการ logic อย่างไร

- สร้าง class สำหรับเรียก API และจัดการข้อมูล

### คำถาม 10.2 วิธีเพิ่ม Favorite Cities feature ยังไง ต้องแก้ไข HTML/CSS/JS ส่วนไหนบ้าง

- เพิ่มปุ่ม favorite และเก็บข้อมูลใน LocalStorage

### คำถาม 10.3 วิธีเพิ่ม Dark Mode ด้วย CSS variables ได้อย่างไร

- ใช้ CSS variables และสลับ class ด้วย JavaScript
